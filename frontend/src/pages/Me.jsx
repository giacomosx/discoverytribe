import React from 'react';
import Layout from "../layout/Layout";
import Feed from "../components/feed/Feed";

const Me = () => {
    return (
        <Layout>
            <Feed/>
        </Layout>
    );
};

export default Me;
